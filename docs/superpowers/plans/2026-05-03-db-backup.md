# DB Backup System Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Automated daily PostgreSQL backup to S3 with email alerts on failure and a tested restore script.

**Architecture:** Cron runs `deployment/backup.sh` at 2am daily on the production server. Script does pg_dump → gzip → S3 upload → prune old backups. On any error, sends email via Gmail SMTP. `deployment/restore.sh` downloads a named or latest backup from S3, confirms with user, and restores with health check.

**Tech Stack:** bash, pg_dump, gzip, aws-cli, curl (Gmail SMTP), docker exec, crontab

---

## File Map

| Action | File |
|---|---|
| Create | `deployment/backup.sh` |
| Create | `deployment/restore.sh` |
| Modify | `deployment/.env.prod.example` |

---

### Task 1: Create backup.sh

**Files:**
- Create: `deployment/backup.sh`

- [ ] **Step 1: Write the script**

```bash
#!/bin/bash
# deployment/backup.sh
set -euo pipefail

# ── Config ────────────────────────────────────────────────────
set -a
source /app/mymemo/.env.prod
set +a

BUCKET="mymemo-backups-prod"
DATE=$(date +%F)
FILENAME="mymemo_${DATE}.sql.gz"
TMP_FILE="/tmp/${FILENAME}"
S3_PREFIX="daily"
RETENTION_DAYS=7

# ── Email on failure ──────────────────────────────────────────
send_failure_email() {
    local error_msg="$1"
    cat > /tmp/mymemo_backup_fail.txt << EOF
From: MyMemo Backup <alaniker1234567@gmail.com>
To: alaniker1234567@gmail.com
Subject: BACKUP FAILED - mymemo_db - ${DATE}

Backup failed on ${DATE} at $(date +%T).

Error: ${error_msg}
Server: mymemo-production-2 (16.58.56.110)
EOF
    curl --silent --ssl-reqd \
        --url "smtps://smtp.gmail.com:465" \
        --user "alaniker1234567@gmail.com:${GMAIL_APP_PASSWORD}" \
        --mail-from "alaniker1234567@gmail.com" \
        --mail-rcpt "alaniker1234567@gmail.com" \
        --upload-file /tmp/mymemo_backup_fail.txt || true
    rm -f /tmp/mymemo_backup_fail.txt
}

trap 'send_failure_email "Script failed at line $LINENO. Check /var/log/mymemo-backup.log"' ERR

# ── 1. pg_dump ────────────────────────────────────────────────
echo "[$(date +%FT%T)] Starting backup: ${FILENAME}"
docker exec mymemo_db pg_dump -U "${DB_USER}" "${DB_NAME}" | gzip > "${TMP_FILE}"
echo "[$(date +%FT%T)] Dump complete: $(du -sh ${TMP_FILE} | cut -f1)"

# ── 2. Upload to S3 ───────────────────────────────────────────
aws s3 cp "${TMP_FILE}" "s3://${BUCKET}/${S3_PREFIX}/${FILENAME}" \
    --region "${AWS_REGION:-us-east-2}"
echo "[$(date +%FT%T)] Uploaded: s3://${BUCKET}/${S3_PREFIX}/${FILENAME}"

# ── 3. Clean local tmp ────────────────────────────────────────
rm -f "${TMP_FILE}"

# ── 4. Prune S3 — keep last RETENTION_DAYS ───────────────────
aws s3 ls "s3://${BUCKET}/${S3_PREFIX}/" \
    | awk '{print $4}' \
    | grep -v '^$' \
    | sort \
    | head -n -${RETENTION_DAYS} \
    | while read -r obj; do
        aws s3 rm "s3://${BUCKET}/${S3_PREFIX}/${obj}" \
            --region "${AWS_REGION:-us-east-2}"
        echo "[$(date +%FT%T)] Pruned: ${obj}"
    done

echo "[$(date +%FT%T)] Backup complete."
```

- [ ] **Step 2: Make executable**

```bash
chmod +x deployment/backup.sh
```

- [ ] **Step 3: Commit**

```bash
git add deployment/backup.sh
git commit -m "feat(backup): add daily pg_dump backup script with S3 upload and email alerts"
```

---

### Task 2: Create restore.sh

**Files:**
- Create: `deployment/restore.sh`

- [ ] **Step 1: Write the script**

```bash
#!/bin/bash
# deployment/restore.sh
# Usage: ./restore.sh [YYYY-MM-DD | latest]
set -euo pipefail

set -a
source /app/mymemo/.env.prod
set +a

BUCKET="mymemo-backups-prod"
S3_PREFIX="daily"
TMP_FILE="/tmp/mymemo_restore.sql.gz"

# ── Resolve which backup ──────────────────────────────────────
if [ -z "${1:-}" ] || [ "$1" = "latest" ]; then
    BACKUP_FILE=$(aws s3 ls "s3://${BUCKET}/${S3_PREFIX}/" \
        | awk '{print $4}' \
        | grep -v '^$' \
        | sort \
        | tail -n 1)
    if [ -z "${BACKUP_FILE}" ]; then
        echo "ERROR: No backups found in s3://${BUCKET}/${S3_PREFIX}/"
        exit 1
    fi
else
    BACKUP_FILE="mymemo_${1}.sql.gz"
fi

# ── Confirmation ──────────────────────────────────────────────
echo "========================================"
echo "  MyMemo Database Restore"
echo "========================================"
echo "  Backup : ${BACKUP_FILE}"
echo "  Target : ${DB_NAME} @ mymemo_db"
echo "========================================"
echo ""
echo "WARNING: This will DESTROY all current data in ${DB_NAME}."
echo ""
read -r -p "Type 'yes' to continue: " CONFIRM
if [ "${CONFIRM}" != "yes" ]; then
    echo "Aborted."
    exit 0
fi

# ── Download ──────────────────────────────────────────────────
echo "[$(date +%FT%T)] Downloading s3://${BUCKET}/${S3_PREFIX}/${BACKUP_FILE}..."
aws s3 cp "s3://${BUCKET}/${S3_PREFIX}/${BACKUP_FILE}" "${TMP_FILE}" \
    --region "${AWS_REGION:-us-east-2}"

# ── Stop services ─────────────────────────────────────────────
echo "[$(date +%FT%T)] Stopping backend services..."
docker stop mymemo_backend mymemo_celery

# ── Drop & recreate DB ────────────────────────────────────────
echo "[$(date +%FT%T)] Dropping database..."
docker exec mymemo_db dropdb -U "${DB_USER}" "${DB_NAME}"
echo "[$(date +%FT%T)] Creating database..."
docker exec mymemo_db createdb -U "${DB_USER}" "${DB_NAME}"

# ── Restore ───────────────────────────────────────────────────
echo "[$(date +%FT%T)] Restoring from ${BACKUP_FILE}..."
gunzip -c "${TMP_FILE}" | docker exec -i mymemo_db psql -U "${DB_USER}" "${DB_NAME}" -q

# ── Restart ───────────────────────────────────────────────────
echo "[$(date +%FT%T)] Restarting services..."
docker start mymemo_backend mymemo_celery

# ── Health check ──────────────────────────────────────────────
echo "[$(date +%FT%T)] Verifying..."
sleep 5
docker exec mymemo_db pg_isready -U "${DB_USER}" \
    && echo "  DB : OK" || echo "  DB : FAIL"
curl -sf http://localhost:8000/health > /dev/null \
    && echo "  API: OK" || echo "  API: FAIL"

# ── Cleanup ───────────────────────────────────────────────────
rm -f "${TMP_FILE}"
echo "[$(date +%FT%T)] Restore complete from ${BACKUP_FILE}."
```

- [ ] **Step 2: Make executable**

```bash
chmod +x deployment/restore.sh
```

- [ ] **Step 3: Commit**

```bash
git add deployment/restore.sh
git commit -m "feat(backup): add restore script with S3 download and health check"
```

---

### Task 3: Update .env.prod.example

**Files:**
- Modify: `deployment/.env.prod.example`

- [ ] **Step 1: Add GMAIL_APP_PASSWORD variable**

Agregar al final de la sección `# ── Seguridad` en `deployment/.env.prod.example`:

```bash
# ── Backups ───────────────────────────────────────────────────
# Gmail App Password para alertas de backup fallido
# Genera en: myaccount.google.com/apppasswords (requiere 2FA activo)
GMAIL_APP_PASSWORD=tu_gmail_app_password
```

- [ ] **Step 2: Commit**

```bash
git add deployment/.env.prod.example
git commit -m "chore: add GMAIL_APP_PASSWORD to env example for backup alerts"
```

---

### Task 4: Deploy scripts al servidor y probar backup

**En el servidor — ejecutar después de `git pull`**

- [ ] **Step 1: Push cambios y hacer pull en servidor**

```bash
# Local
git push

# Servidor
ssh mymemo
cd /app/mymemo
git pull
```

- [ ] **Step 2: Agregar GMAIL_APP_PASSWORD a .env.prod en el servidor**

```bash
# En el servidor
echo "GMAIL_APP_PASSWORD=tu_app_password_aqui" >> /app/mymemo/.env.prod
```

Genera el App Password en: `myaccount.google.com/apppasswords`
Nombre sugerido: "MyMemo Backup"

- [ ] **Step 3: Verificar que aws cli tiene credenciales**

```bash
# En el servidor
aws s3 ls s3://mymemo-backups-prod/ --region us-east-2
```

Esperado: sin error (puede estar vacío)

- [ ] **Step 4: Ejecutar backup manualmente**

```bash
bash /app/mymemo/deployment/backup.sh
```

Esperado:
```
[2026-05-03T02:00:01] Starting backup: mymemo_2026-05-03.sql.gz
[2026-05-03T02:00:15] Dump complete: 2.3M
[2026-05-03T02:00:22] Uploaded: s3://mymemo-backups-prod/daily/mymemo_2026-05-03.sql.gz
[2026-05-03T02:00:23] Backup complete.
```

- [ ] **Step 5: Verificar archivo en S3**

```bash
aws s3 ls s3://mymemo-backups-prod/daily/ --region us-east-2
```

Esperado: `mymemo_2026-05-03.sql.gz` listado

---

### Task 5: Instalar cron en servidor

- [ ] **Step 1: Abrir crontab**

```bash
# En el servidor
crontab -e
```

- [ ] **Step 2: Agregar línea**

```cron
0 2 * * * /app/mymemo/deployment/backup.sh >> /var/log/mymemo-backup.log 2>&1
```

- [ ] **Step 3: Verificar cron instalado**

```bash
crontab -l
```

Esperado: la línea aparece en output

- [ ] **Step 4: Verificar log después de primera ejecución automática**

Al día siguiente de las 2am:
```bash
tail -50 /var/log/mymemo-backup.log
```

---

### Task 6: Configurar S3 Lifecycle rule (red de seguridad)

- [ ] **Step 1: Crear archivo de política**

Crear `/tmp/lifecycle.json` en local o servidor:

```json
{
  "Rules": [
    {
      "ID": "expire-old-backups",
      "Status": "Enabled",
      "Filter": {
        "Prefix": "daily/"
      },
      "Expiration": {
        "Days": 10
      }
    }
  ]
}
```

- [ ] **Step 2: Aplicar política al bucket**

```bash
aws s3api put-bucket-lifecycle-configuration \
    --bucket mymemo-backups-prod \
    --lifecycle-configuration file:///tmp/lifecycle.json \
    --region us-east-2
```

Esperado: sin output (éxito silencioso)

- [ ] **Step 3: Verificar política aplicada**

```bash
aws s3api get-bucket-lifecycle-configuration \
    --bucket mymemo-backups-prod \
    --region us-east-2
```

Esperado: JSON con la regla `expire-old-backups`

---

### Task 7: Probar restore

- [ ] **Step 1: Ejecutar restore con el backup recién creado**

```bash
# En el servidor
bash /app/mymemo/deployment/restore.sh latest
```

Esperado: muestra prompt de confirmación con nombre del backup

- [ ] **Step 2: Confirmar con 'yes'**

Esperado:
```
[...] Downloading s3://mymemo-backups-prod/daily/mymemo_2026-05-03.sql.gz...
[...] Stopping backend services...
[...] Dropping database...
[...] Creating database...
[...] Restoring from mymemo_2026-05-03.sql.gz...
[...] Restarting services...
[...] Verifying...
  DB : OK
  API: OK
[...] Restore complete from mymemo_2026-05-03.sql.gz.
```

- [ ] **Step 3: Verificar app funciona post-restore**

```bash
curl https://mymemo-app.duckdns.org/health
```

Esperado: `{"status":"healthy",...}`
